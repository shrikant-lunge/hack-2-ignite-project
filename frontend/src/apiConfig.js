// Unified API configuration.
// By using an absolute URL with the correct protocol, this works perfectly on:
// 1. Localhost (desktop)
// 2. Local IP (mobile/network)
// 3. Ngrok/Public tunnels (single tunnel only)

export const API_BASE_URL = 'http://127.0.0.1:5000'; // Absolute URL with protocol
